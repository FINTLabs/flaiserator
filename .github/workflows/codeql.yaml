name: CodeQL

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    - cron: "30 5 * * 0,3,5"

permissions:
  actions: read
  contents: read
  pull-requests: read
  security-events: write

jobs:
  detect-changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      actions: ${{ steps.filter.outputs.actions }}
      java_kotlin: ${{ steps.filter.outputs.java_kotlin }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            actions:
              - ".github/**"
            java_kotlin:
              - "src/**"
              - "buildSrc/**"
              - "gradle/**"
              - "build.gradle.kts"
              - "settings.gradle.kts"
              - "gradle.properties"

  analyze-actions:
    name: CodeQL (actions)
    runs-on: ubuntu-latest
    needs: detect-changes
    env:
      run_analysis: ${{ github.event_name == 'schedule' || needs.detect-changes.outputs.actions == 'true' }}
    steps:
      - if: env.run_analysis == 'true'
        uses: actions/checkout@v7

      - if: env.run_analysis == 'true'
        uses: github/codeql-action/init@v4
        with:
          languages: actions

      - if: env.run_analysis == 'true'
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:actions

  analyze-java-kotlin:
    name: CodeQL (java-kotlin)
    runs-on: ubuntu-latest
    needs: detect-changes
    env:
      run_analysis: ${{ github.event_name == 'schedule' || needs.detect-changes.outputs.java_kotlin == 'true' }}
    steps:
      - if: env.run_analysis == 'true'
        uses: actions/checkout@v7

      - if: env.run_analysis == 'true'
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - if: env.run_analysis == 'true'
        uses: github/codeql-action/init@v4
        with:
          languages: java-kotlin
          build-mode: autobuild

      - if: env.run_analysis == 'true'
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:java-kotlin
